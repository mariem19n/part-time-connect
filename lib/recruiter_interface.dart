import 'package:flutter/material.dart';

class RecruiterInterface extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Recruiter')),
      body: Center(child: Text('Recruiter Interface')),
    );
  }
}